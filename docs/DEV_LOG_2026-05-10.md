# SimFEA Studio 开发日志 - 2026-05-10

## 目标

继续优化前端结构，并打通 VTK.js 最小视图入口。

## 前端组件化

新增：

```text
app/types.ts
app/components/ResultEvidenceView.vue
app/components/VtkResultViewport.vue
```

调整：

- `App.vue` 只保留工作台状态、API 调用和运行控制。
- `ResultEvidenceView.vue` 负责证据图和视图切换。
- `VtkResultViewport.vue` 负责加载归档 `.vtk` 文件并渲染。
- 删除模板残留 `HelloWorld.vue`。

## VTK 最小链路

后端生成：

```text
.simfea/runs/<run_id>/artifacts/cantilever_result.vtk
```

并在摘要中写入：

```json
{
  "visualization": {
    "vtk_artifact": "artifacts/cantilever_result.vtk"
  }
}
```

前端通过接口读取：

```text
GET /v1/runs/{run_id}/artifacts/{artifact_path}
```

然后由 VTK.js 渲染。

## 新增依赖

```json
"@kitware/vtk.js": "^34.11.1"
```

安装：

```powershell
pnpm install
```

## 验证

- Vue SFC 编译检查通过。
- 后端 `py_compile` 通过。
- sidecar 可重新打包。
- Vite 构建需要在非沙箱环境运行，因为 esbuild 子进程在沙箱内可能触发权限限制。

## 下一步

- 运行桌面应用，验证 `VTK 视图` 按钮可用。
- 把后端单文件拆成配置、Runner、归档、可视化模块。
- 接 CalculiX 悬臂梁真实算例。
- 将真实结果转换为 `.vtk/.vtu` 并复用 VTK 视图。
