# Dev Log 2026-06-15 — 后端架构重构：路由拆分、执行模块提取、API 客户端收口

## 核心决策

### 主模块瘦身：`main.py` 从 350+ 行 → ~150 行

之前 `main.py` 是万能文件：FastAPI app 工厂 + 路由定义 + 执行逻辑 + 工具函数，全部混在一起。随着功能增加，文件膨胀到 350+ 行，任何改动都需要在巨大的文件中翻找。

今天完成三步拆分，让 `main.py` 只做一件事：**App 工厂和生命周期管理**。

---

## 完成内容

### 1. 提取 runs 路由 → `src/backends/routers/runs.py`

**原状：** `main.py` 包含 15 个 `/v1/runs/*` 端点（list、get、demo、slurm-demo、solver-run、workflow、note、report、cancel、events 等），占 700+ 行。

**拆分后：**
- `src/backends/routers/runs.py` — 808 行，独立路由器，包含所有 runs 端点
- `src/backends/state.py` — 18 行，共享可变状态（`remote_runs` 内存注册表），供 main.py 和 routers/runs.py 共用
- `main.py` — 仅保留 `app.include_router(runs_router)`

**关键设计：**
- `remote_runs: dict[str, RemoteRun]` 放在独立 `state.py`，避免 main.py 和 runs router 循环导入
- runs router 通过 `try/except ImportError` 懒导入 execution 函数（demo、cancel、workflow 启动等），保持 router 不直接依赖执行层

### 2. 提取执行模块 → `src/backends/execution/__init__.py`

**原状：** `main.py` 包含 `execute_local_run`、`execute_remote_run`、`execute_slurm_run`、`execute_local_workflow_run`、`_run_local_command`、FRD 指标提取、STA 进度轮询等 900+ 行执行逻辑。

**拆分后：**
- `src/backends/execution/__init__.py` — 1054 行，纯执行层，无 FastAPI 依赖
- `main.py` — 通过 `from .execution import (...)` 导入需要的函数

**关键设计：**
- execution 模块**不导入** `fastapi`、`sse_starlette`、`EventSourceResponse`，符合 `check_backend_boundaries.py` 边界规则
- 事件发射函数 `emit_remote_event`、`emit_finished_event` 保留在 execution 模块，因为它们是执行过程的一部分

### 3. 前端 experiment API 收口 + `path`-style 参数编码修复

**原状：** `ExperimentLab.vue`、`ResearchView.vue`、`SolverDevView.vue`、`useExperimentFiles.ts` 各自直接 `fetch()` experiment 端点，没有统一封装。

**问题发现：**
- typed client 的 `encodeURIComponent` 会把文件路径中的 `/` 编码为 `%2F`
- 导致 `/v1/experiment/files/learning/benchmarks/rod_tension/problem.md` 变成 `...%2Frod_tension%2Fproblem.md`
- FastAPI 的 `{file_path:path}` 参数无法正确匹配，返回 404

**修复方案：**
- `app/api/client.ts`：新增 `pathParams` 字段，标记哪些参数需要 `encodeURI`（保留 `/`）而非 `encodeURIComponent`
- `app/api/simfeaClient.ts`：experiment `filePath` 标记为 `pathParams: ['filePath']`
- `listExperimentFiles`：响应归一化，把 `benchmarks/...` 路径前缀统一补成 `learning/benchmarks/...`

**新增回归测试：**
- `app/api/client.test.ts` — 60 行，验证 `encodeURIComponent` vs `encodeURI` 分支
- `app/api/simfeaClient.test.ts` — 57 行，验证 experiment 端点 URL 生成

### 4. 死代码清理

**`docker_cli_executable()` 重复定义：**
- execution 模块拆分后，该函数已移入 `execution/__init__.py`
- main.py 中原副本变成死代码，删除 9 行

**未使用 import 清理（opencode-quality-lane 分支）：**
- 删除 `json`、`subprocess`、`shutil`、`uuid`、`datetime`、`timezone`、`TypedDict`
- 删除 `Body`、`File`、`Form`、`HTTPException`、`UploadFile`、`FileResponse`、`EventSourceResponse`
- 删除全部 `simfea_api.*` 别名导入（`ComputeNode`、`SolverDefinition`、`RemoteRun`、`run_metadata` 等）
- 删除 `inference.infer_text_api` 导入
- 删除 `_helpers` 导入（`get_compute_node`、`get_solver` 等）
- 删除 `state.remote_runs` 导入（已不需要）
- `main.py`：358 行 → 154 行，零逻辑变化

### 5. 运行时冒烟测试

新增 `src/backends/tests/test_smoke_post_refactor.py`：
- `GET /v1/config` — 配置接口
- `GET /v1/solvers` — 求解器列表
- `GET /v1/runs` — 运行归档
- `GET /v1/experiment/files` — 实验文件列表
- `GET /v1/experiment/files/{file_path:path}` — path-style 参数读取

全部通过 TestClient 验证，确认重构后 API 行为未变。

### 6. 其他小修复

- `experiments.py` 超时错误消息："30 seconds" → "60 seconds"（与实际 `timeout=60` 一致）

---

## 文件变更

### 新建
- `src/backends/routers/runs.py` — runs 路由（15 个端点）
- `src/backends/execution/__init__.py` — 执行模块（local/remote/slurm/workflow/docker 运行器）
- `src/backends/state.py` — 共享状态（`remote_runs`）
- `app/api/client.test.ts` — client pathParams 回归测试
- `app/api/simfeaClient.test.ts` — simfeaClient experiment 回归测试
- `src/backends/tests/test_smoke_post_refactor.py` — 重构后冒烟测试

### 修改
- `src/backends/main.py` — 删除 700+ 行路由和执行逻辑，仅剩 App 工厂
- `src/backends/routers/experiments.py` — 超时消息修正
- `app/api/client.ts` — 新增 `pathParams` 支持
- `app/api/contracts.ts` — 新增 experiment 端点契约
- `app/api/simfeaClient.ts` — 新增 experiment 方法 + pathParams
- `app/App.vue`、`ExperimentLab.vue`、`ResearchView.vue`、`SolverDevView.vue`、`useExperimentFiles.ts` — 改用 typed client

### 删除
- `src/backends/main.py` 中死掉的 `docker_cli_executable()` 函数
- `src/backends/main.py` 中 200+ 行未使用 import

---

## 验证

```bash
# Python 单元测试
python -m unittest discover -s src/backends/tests -v
# Ran 125 tests in 0.592s — OK

# 模块边界检查
python scripts/check_backend_boundaries.py
# OK: 24 modules, no boundary violations

# Vitest
npx vitest run
# Test Files  6 passed (6), Tests 35 passed (35)

# Biome
npx biome check app/
# Checked 47 files, No fixes applied

# 空白字符检查
git diff --check
# Clean
```

---

## 架构现状

```text
src/backends/
├── main.py              # App 工厂 + 生命周期（~150 行）
├── state.py             # 共享内存状态（remote_runs）
├── cli.py               # pip 入口
├── execution/           # 执行层（local/remote/slurm/workflow/docker）
│   └── __init__.py      # 1054 行，纯执行逻辑
├── routers/             # 路由层
│   ├── _helpers.py      # 共享辅助函数
│   ├── benchmarks.py
│   ├── compute_nodes.py
│   ├── config.py
│   ├── experiments.py
│   ├── knowledge.py
│   ├── preflight.py
│   ├── runs.py          # 808 行，运行相关端点
│   └── toolchain.py
└── simfea_api/          # 服务层
    ├── config.py
    ├── run_archive.py
    ├── runners/         # ssh/slurm/solver/workflow/remote_files
    ├── learning.py
    ├── results.py
    └── ...
```

边界规则：
- `simfea_api/` 不导入 `main`（无循环导入）
- `runners/` 不导入 `fastapi` 或 `sse_starlette`（纯执行）
- `execution/` 不导入 `fastapi`（纯执行）

---

---

## 附录：功能全面审计（同日追加）

> 审计背景：用户反馈"发展有点快，功能没有赶上"，且 README 截图显示异常。因此对前后端所有视图和执行能力做一次诚实盘点。

### 前端视图真实状态

| 视图 | 状态 | 说明 |
|------|------|------|
| **作业区 (Composer)** | ✅ 完整 | Workflow 树、文件上传、预检查、提交、队列、状态栏全部可用 |
| **学习库 (Learning Library)** | ✅ 完整 | 筛选、搜索、导出、标签、详情查看 |
| **基准实验室 (Benchmark Lab)** | ✅ 完整 | 13 个案例列表、对比表、误差条、问题描述渲染 |
| **方法实验室 (Method Lab)** | ⚠️ **部分** | 统计卡片和路线图是真实的；**证据矩阵有 3 列永远是 `?`**（systemDiagram、singlePathCall、diffComparison 写死为 null）；**AI 门控是静态文案**，不随运行时变化 |
| **实验 (Experiment Lab)** | ✅ 完整 | 文件树、编辑器、保存、运行（stdout/stderr） |
| **研究 (Research View)** | ✅ 完整 | Markdown CRUD、新建、预览 |
| **运行详情 (Run Detail)** | ✅ 完整 | 引导问题、笔记保存、报告生成、证据查看、VTK 可视化 |
| **AI 问答 (AiChat)** | ✅ 完整 | RAG 聊天、知识库文档选择、多 session 历史（localStorage） |
| **工具链 (Toolchain)** | ✅ 完整 | 求解器发现、路径配置、验证测试、安装任务（SSE 进度） |
| **求解器开发 (SolverDev)** | ❌ **骨架** | 名字叫"求解器开发"，实际只是 `learning/solver-dev/` 目录的**只读 Markdown 查看器**，没有编辑、没有运行、没有构建，比 Experiment Lab 功能还少 |

### 后端执行能力真实状态

| 执行器 | 状态 | 说明 |
|--------|------|------|
| **LocalRunner** | ✅ 完整 | `subprocess.run` + 线程池，Windows 防死锁，STA 进度轮询，FRD 指标提取 |
| **SSHRunner** | ✅ 完整 | `ssh` + `scp`，远程执行、artifact 下载 |
| **SlurmRunner** | ✅ 完整 | `sbatch` 提交、`squeue` 轮询、`sacct` 完成状态、日志同步、取消 |
| **SolverRunner** | ✅ 完整 | 写输入 → pre → solver → post → artifact 收集 → FRD→VTK |
| **WorkflowRunner** | ✅ 完整 | `freecad-prepomax` 已端到端验证，自定义 workflow 也通 |
| **DockerRunner** | ⚠️ 有代码 | `_execute_docker_run` 存在但依赖本地 Docker 容器 `simfea-solvers`，未验证 |
| **FRD→VTK 转换** | ✅ 完整 | `frd_to_vtk.py` 25277 行，支持多步、PVD、VTU、应力/位移指标提取 |
| **AI 文本推理** | ⚠️ 有代码 | `infer_text_api.py` 存在，但不确定是否有真实模型后端 |

### 求解器支持真实状态

| 求解器 | 状态 | 说明 |
|--------|------|------|
| **CalculiX** | ✅ **已验证** | 悬臂梁端到端，FRD→VTK，指标摘要，工具链配置 |
| **freecad-prepomax workflow** | ✅ **已验证** | 两步链式，20 个产物统一归档 |
| **PrePoMax regeneration** | ✅ **已验证** | STEP→Gmsh→CalculiX→VTK 热力图 |
| **FreeCAD** | ⚠️ 骨架 | `FreeCADCmd` 无头宏，smoke case 生成 `.FCStd/.step`，未接真实 case |
| **OpenFOAM** | ❌ 骨架 | 只有适配器定义，没有真实 case 文件 |
| **Elmer** | ❌ 骨架 | 只有适配器定义，没有真实 case 文件 |
| **Abaqus** | ❌ 骨架 | UI 里有选项，但后端没有定义 |

### README 截图问题

| 问题 | 根因 |
|------|------|
| 截图与当前 UI 不符 | 截图是 5 月 23 日的，缺少 6 月新增的 Method Lab、Experiment、Research 三个视图 |
| 中文路径可能渲染失败 | `docs/项目桌面版本主界面.png` 等文件名含中文，部分浏览器/GitHub App 渲染不稳定 |
| 截图已过时 | 5 张截图里没有新版导航栏的 10 个入口 |

### 核心问题总结

1. **SolverDevView（求解器开发）名不副实** — 用户点进来期待能写/跑/调试求解器代码，结果只能看文档。
2. **Method Lab 证据矩阵做空** — 3 列永远是 `?`，没有真正加载 case 详情去检测证据是否存在。AI 门控是写死的 6 张卡片。
3. **OpenFOAM / Elmer / Abaqus 在 UI 里暴露但未实现** — 用户可选但跑不了。
4. **截图严重过时** — README 展示的是 5 月的旧界面。

### 建议的下一步

**优先级 1：补齐"名不副实"的功能**
- SolverDevView 要么删掉重做成真正的求解器开发环境（集成 Experiment Lab 的编辑+运行能力），要么改名成"求解器文档"

**优先级 2：Method Lab 证据矩阵做实**
- 真正调用 `api.getBenchmarkCase()` 加载每个 case 的详情，检测 `systemDiagram`、`singlePathCall`、`diffComparison` 是否存在，而不是写死 null

**优先级 3：截图更新**
- 用英文文件名重新截图，替换 README 中的 5 张旧图

**优先级 4：求解器骨架标注**
- OpenFOAM、Elmer、Abaqus 在 UI 里应该标记为"即将支持"或灰色不可选，避免用户误点

---

## 下一步

- [ ] 前端 typed client 覆盖剩余直接 `fetch()` 调用（runs、benchmarks、compute_nodes）
- [ ] OpenFOAM / Elmer 真实 case 接入
- [ ] 新机器 `pip install && simfea-studio` 最短路径计时验证
- [ ] **(新增)** SolverDevView 名实相符改造或更名
- [ ] **(新增)** Method Lab 证据矩阵真实化
- [ ] **(新增)** README 截图更新为英文路径 + 当前 UI
