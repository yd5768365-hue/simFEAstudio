# SimFEA Studio 开发日志 - 2026-05-19

## 本次目标

根据新需求重构作业区交互：

1. 顶部模式切换：`单求解器模式` / `启动链模式`
2. 文件选择方式切换：`单个文件` / `实例文件夹`
3. 提交后任务队列卡片：进度条、阶段状态、可折叠实时日志、多任务并排

## 已完成内容

1. 首页重复状态去重：
   - 删除了头部重复的红色状态徽章，仅保留右侧状态区作为唯一状态来源。
2. 新交互的数据结构已接入 `app/App.vue`：
   - `JobMode` / `UploadMode`
   - `PipelineStep`
   - `QueueItem`
   - `queueItems` 队列状态管理方法（创建、更新、日志折叠）
3. 提交动作逻辑已增强：
   - 提交前校验侧车连接与运行状态
   - 支持按模式触发：
     - 单求解器：`startSolverRunAction(...)`
     - 启动链：`startFreecadPrepomaxWorkflowAction()`
   - 基于 `remoteStatus` 的 `watch` 更新任务卡进度与阶段

## 过程中出现的问题与处理

本次中途出现字符串引号破坏（`App.vue` 多处 `''`/`'` 混乱），导致 Vue SFC 语法错误。

已处理：

1. 定位并修复关键语法错误：
   - `apiBaseUrl` 拼接字符串
   - `status` 初始化中的空字符串字段
   - `handleFileInputChange` 的 `firstPath`/`instanceFolderName` 默认值
   - `setComputeNodes(... default_compute_node ?? '')`
   - `ResultEvidenceView` 的 `report-preview` 空字符串
2. 重新构建验证通过：
   - `npm.cmd run build` 成功（2026-05-19）

## 当前状态

1. 项目处于可构建状态。
2. 作业区“完整 UI 重排”（模式区、上传区、链路区、队列区样式）尚未全部完成，当前处于脚本层逻辑已接入、模板层未完全对齐状态。

## 下一步（明确可执行）

1. 仅在 `app/App.vue` 完成作业区模板重排（不动后端）。
2. 在 `app/style.css` 追加模式开关、链路步骤卡、队列卡样式。
3. 构建验证 + 浏览器核验（布局、交互、响应式）。
4. 记录最终截图与验收点到下一条开发日志。

